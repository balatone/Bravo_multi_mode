--[[
    bravo++/dispatch/init.lua - Dispatch sub-package entry point

    Allows require("bravo++.dispatch") to resolve to the dispatch facade
    when the sub-package directory structure is used.
]]
return require("bravo++.dispatch.dispatch")
